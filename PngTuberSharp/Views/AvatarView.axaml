<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="1280" d:DesignHeight="720"
			 xmlns:vm="clr-namespace:PngTuberSharp.ViewModels"
			  x:DataType="vm:AvatarViewModel"
             x:Class="PngTuberSharp.Views.AvatarView">
	<Grid >
		<TextBlock x:Name="fpsCounter" Text="{Binding Fps}"></TextBlock>

		<Image  Stretch="Uniform"
	   HorizontalAlignment="Stretch"
	   VerticalAlignment="Stretch"
				Source="{Binding Image}" Name="image"></Image>
		<!--<ListBox Margin="0,50,0,0" Background="Transparent" ItemsSource="{Binding Throwables}" HorizontalAlignment="Left">
			<ItemsControl.ItemTemplate>
				<DataTemplate>
					<StackPanel>
						<TextBlock Text="{Binding X}"></TextBlock>
						<TextBlock Text="{Binding Y}"></TextBlock>
						<TextBlock Text="{Binding Rotation}"></TextBlock>
						<Image Source="{Binding Image}}" Height="50" Width="50"></Image>
					</StackPanel>
				</DataTemplate>
			</ItemsControl.ItemTemplate>
		</ListBox>-->
	</Grid>
</UserControl>
