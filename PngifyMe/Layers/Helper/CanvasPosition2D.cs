namespace PngifyMe.Layers.Helper;

/// <summary>
/// Helper type for canvas position (X, Y). Use with [CanvasPosition] for a single attribute instead of separate X/Y properties.
/// </summary>
public sealed class CanvasPosition2D
{
    public float X { get; set; }
    public float Y { get; set; }
}
